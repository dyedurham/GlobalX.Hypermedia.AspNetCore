version: 2
jobs:
  build:
    docker:
      - image: microsoft/aspnetcore-build:2.0
    steps:
      - checkout
      - run: find .
      - run: cd src
      - run: dotnet restore
      - run: dotnet pack GlobalX.Hypermedia.AspNetCore/GlobalX.Hypermedia.AspNetCore.csproj --include-source --include-symbols -o ../../nuget